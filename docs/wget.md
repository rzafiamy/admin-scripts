# 🌐✨ wget.sh — Pretty Terminal Downloader

A lightweight Bash script that wraps `wget` with colorful console output, a spinner, and a clean banner.
Drop it into your toolbox and download any URL with style.

---

## 🚀 Features

* 🎨 Colorful header and progress visuals
* ⏱ Smart timeouts and retry settings
* 🔄 Resume (`-c`) support
* 🤫 Quiet mode with friendly output
* 🌍 Works anywhere you have Bash + wget

---

## 📥 Usage

```bash
./wget.sh <url>
```

Example:

```bash
./wget.sh https://huggingface.co/.../file.gguf
```

---

## 🔧 Installation

```bash
chmod +x wget.sh
```

Place it anywhere in your PATH if you want global access.

---

## 🧩 Notes

* Uses ANSI escape codes—best viewed in a modern terminal.
* Requires `wget` installed on your system.

---

## 🏁 Have fun downloading!

“Tools should be simple. Beauty is optional, but delightful.” 💫
